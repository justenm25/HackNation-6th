# Runtime dependencies for the E. coli bundle app (Hugging Face Docker Space).
# scikit-learn is pinned to the version the E. coli bundle models were serialized
# with — a mismatch silently breaks predict_proba. Do not change without re-checking
# models/ecoli-bundle.
streamlit>=1.36
pandas>=2.0
scikit-learn==1.9.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
PyYAML>=6.0
joblib>=1.3
