# Streamlit Community Cloud deployment (real E. coli models, precomputed demo TSV).
# Entry point: streamlit_app.py at the repository root.
# scikit-learn is pinned to the version used to serialize models/ecoli-bundle.
streamlit>=1.36
pandas>=2.0
scikit-learn==1.9.0
matplotlib>=3.7
numpy>=1.24
scipy>=1.10
PyYAML>=6.0
pyarrow>=14.0
joblib>=1.3
